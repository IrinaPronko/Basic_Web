let temp_c = +prompt('Введите температуру в градусах Цельсия');
let temp_f = Math.floor((9 / 5 * temp_c + 32) * 100) / 100;
alert(`Градусы Цельсия: ${temp_c} соотвествуют\n градусам Фаренгейта: ${temp_f}`)